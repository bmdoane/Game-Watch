'use strict'

app.controller('TeamCtrl', function($scope, TeamsFactory) {
	console.log("Can you hear team?")
	// Use route params team/:id in app.js routes
	// Save id to variable
	// Will send http.get own route params with :id	
	
})